[package]
name = "mt5_session"
version = "0.1.0"
edition = "2024"

[dependencies]
mt5_native = { path = "../mt5-native" }
getrandom = { version = "0.2", optional = true }

[features]
live = ["mt5_native/live", "dep:getrandom"]

[[example]]
name = "auth_probe"
required-features = ["live"]
